{% extends "AlfitraCptBundle::layout.html.twig" %}

{% block body %}
    <div class="container">
    <h1 class="text-center">Créer un nouvel Evenement</h1>
    <div class="col-md-6 col-md-offset-3">

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <a href="{{ path('evenement_index') }}" class="btn btn-warning">Revenir à la liste</a>
        <input type="submit" value="Créer" class="btn btn-success"/>
    {{ form_end(form) }}
    </div>
    </div>

{% endblock %}
